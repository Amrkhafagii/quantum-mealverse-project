
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const ExerciseLevelList: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Exercise Level List</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Exercise level list functionality coming soon...</p>
      </CardContent>
    </Card>
  );
};

export default ExerciseLevelList;
